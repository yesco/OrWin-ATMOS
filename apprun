# TODO: subdirs each an app?

rm -f app-*.s app-*.ext app-*.reg app-*.o
rm -f apps.reg apps.ext
rm -f apprun.x apprun.sim apprun.tap

ls -1 app-*.c | grep -iv orwin.c | sed 's/\.c//' \
| xargs -l1 ./appwrap

./appwrap atmos

cat app-*.reg > apps.reg
cat app-*.ext > apps.ext

#cl65 -t atmos apprun.c app-*.o -o apprun.x

cl65 -t sim6502 apprun.c app-*.o -o apprun.x

echo
echo 

sim65 ./apprun.sim

