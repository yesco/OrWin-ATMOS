# TODO: ALL
# ls -1 *.c | grep -iv orwin.c | sed 's/\.c//' | xargs -l1 ./appwrap

rm app-*.s app-*.ext app-*.reg apprun.x app-*.o apps.reg apps.ext

./appwrap atmos

cat app-*.reg > apps.reg
cat app-*.ext > apps.ext

cl65 -t atmos apprun.c app-*.o -o apprun.x

sim65 ./apprun.x
